<h1>NFL Players</h1>

<% @teams.each do |team| %>
  <h2>
    <%= team.name %>
  </h2>
  <ul>
    <% team.find_players.each do |player| %>
      <li>
        <%= link_to "#{player.name} | #{player.team}", player %>
      </li>
    <% end %>
  </ul>
<% end %>
